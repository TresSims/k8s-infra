---
apiVersion: helm.toolkit.fluxcd.io/v2
kind: HelmRelease
metadata:
  name: nfs-csi
  namespace: nfs-csi
spec:
  interval: 10m
  chart:
    spec:
      chart: csi-driver-nfs
      version: '4.12.0'
      sourceRef:
        kind: HelmRepository
        name: nfs-csi-helm
        namespace: kube-system
      interval: 10m
  values:
    hubble:
      enabled: true

